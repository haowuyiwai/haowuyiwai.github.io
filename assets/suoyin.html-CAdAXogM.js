import{_ as s,c as a,b as e,o}from"./app-CqN-hAy0.js";const t="/assets/02-CTdJ8FC1.png",l="/assets/03-Drp87-ZK.png",p={};function c(i,n){return o(),a("div",null,n[0]||(n[0]=[e('<p>[toc]</p><h1 id="一、索引" tabindex="-1"><a class="header-anchor" href="#一、索引"><span>一、索引</span></a></h1><p>MySQL官方对索引I的定义为：<strong>索引（Index）是帮助MySQL高效获取数据的数据结构。</strong></p><p>索引的本质：索引是数据结构。可以简单理解为“排好序的快速查找数据结构”，满足特定查找算法。这些数据结构以某种方式指向数据，这样就可以在这些数据结构的基础上实现高级查找算法。</p><p>数据库的索引好比是一本书的目录，能加快数据库的查询速度。</p><ul><li>一般来说索引本身也很大，不可能全部存储在内存中，因此索引往往是存储在磁盘上的文件中的（可能存储在单独的索引文件中，也可能和数据一起存储在数据文件中）。</li></ul><p>索引底层的数据结构存在很多类型，常见的有：B树、B+树和哈希。在MySQL中，无论是Innodb还是MyIsan都使用了B+树作为索引结构。</p><p><img src="'+t+`" alt=""></p><p>MySQL的索引和数据就是位于存储引擎中。MySQL 创建索引的基本结构，其中的参数说明如下：</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> table_name <span class="token punctuation">(</span></span>
<span class="line">    col_name data_type<span class="token punctuation">,</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span></span>
<span class="line">    <span class="token punctuation">[</span><span class="token keyword">UNIQUE</span> <span class="token operator">|</span> FULLTEXT <span class="token punctuation">]</span> <span class="token punctuation">[</span><span class="token keyword">INDEX</span> <span class="token operator">|</span> <span class="token keyword">KEY</span><span class="token punctuation">]</span> <span class="token punctuation">[</span>index_name<span class="token punctuation">]</span> <span class="token punctuation">(</span>col_name <span class="token punctuation">[</span>length<span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token punctuation">[</span><span class="token keyword">ASC</span> <span class="token operator">|</span> <span class="token keyword">DESC</span><span class="token punctuation">]</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span></span>
<span class="line"><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>参数解释：</p><ul><li><strong>UNIQUE</strong>：可选参数，表示唯一索引，确保索引列中的所有值是唯一的。</li><li><strong>FULLTEXT</strong>：可选参数，表示全文索引，用于加速文本字段的搜索（适用于 InnoDB 和 MyISAM 存储引擎的 <code>CHAR</code>、<code>VARCHAR</code> 和 <code>TEXT</code> 列）。</li><li><strong>INDEX | KEY</strong>：<code>INDEX</code> 和 <code>KEY</code> 是同义词，用于指定创建索引的关键字。</li><li><strong>index_name</strong>：指定索引的名称（可选）。如果不指定，MySQL 默认使用列名作为索引名。</li><li><strong>col_name</strong>：索引的列名，必须是表中定义的列。</li><li><strong>length</strong>：可选参数，用于指定索引的长度（仅对字符串类型的字段有效）。部分索引可以节省存储空间并提高查询速度。</li><li><strong>ASC | DESC</strong>：指定索引值的排序顺序（升序或降序）。在 MySQL 中，B+Tree 索引默认使用升序排序，但排序顺序在大多数情况下对查询优化影响不大。</li></ul><p>例如，创建一个具有唯一索引和全文索引的表语句如下：</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> example_table <span class="token punctuation">(</span></span>
<span class="line">    id <span class="token keyword">INT</span><span class="token punctuation">,</span></span>
<span class="line">    name <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    description <span class="token keyword">TEXT</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token keyword">UNIQUE</span> <span class="token keyword">INDEX</span> unique_name_index <span class="token punctuation">(</span>name<span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    FULLTEXT <span class="token keyword">INDEX</span> description_text_index <span class="token punctuation">(</span>description<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><hr><h1 id="二、索引分类" tabindex="-1"><a class="header-anchor" href="#二、索引分类"><span>二、索引分类</span></a></h1><p>可以按照四个角度来分类索引。</p><ul><li>按数据结构分类：<strong>B+tree索引、Hash索引、全文索引</strong>。</li><li>按<strong>物理实现</strong>分类：<strong>聚簇索引（主键索引）、二级索引（辅助索引）</strong>。</li><li>按<strong>功能逻辑</strong>分类：<strong>普通索引、唯一索引、主键索引、前缀索引</strong>。</li><li>按<strong>字段个数</strong>分类：<strong>单列索引、联合索引</strong>。</li></ul><h2 id="_1、按物理存储分类" tabindex="-1"><a class="header-anchor" href="#_1、按物理存储分类"><span>1、按物理存储分类</span></a></h2><p>按照物理实现，索引分为聚簇索引和非聚簇索引（也称二级索引或辅助索引）。</p><h3 id="a-聚簇索引" tabindex="-1"><a class="header-anchor" href="#a-聚簇索引"><span>a.聚簇索引</span></a></h3><p>**所有的用户数据都存在了叶子节点，数据即索引，索引即数据。**聚簇索引将数据存储与索引结构结合在一起，索引的叶子节点即存储了数据行的全部信息。这意味着通过聚簇索引查询到的索引项就等于查询到完整的数据行，无需额外访问。</p><p>使用记录主键值的大小进行记录和页的排序，这包括三个方面的含义：</p><ul><li>页内的记录是按照主键的大小顺序排成一个单向链表。</li><li>各个存放用户记录的页也是根据页中用户记录的主键大小顺序排除一个双向链表。存放目录项记录的页分为不同层次，在同一层次中的页是根据页中目录项记录的主键大小顺序拍成的一个双向链表。</li></ul><p>在 InnoDB 中，如果表定义了主键，系统会自动将主键列作为聚簇索引的索引键；如果没有主键，则会选择第一个非空的唯一列；如果没有符合条件的唯一列，系统会创建一个隐式的自增 <code>rowid</code> 列作为聚簇索引键。</p><blockquote><p>唯一性：<strong>每个表只能有一个聚簇索引，因为数据只能按照一种顺序存储在物理结构中。</strong></p></blockquote><p>B+树的叶子节点存储的完整的用户数据。</p><blockquote><p><strong>优点</strong>：</p></blockquote><p><strong>数据访问更快</strong>：因为聚簇索引将索引和数据保存在同一个B+树中，因此从聚簇索引中获取数据比非聚簇索引更快。</p><p><strong>聚簇索引对于主键的排序查找和范围查找速度非常快</strong>：数据按照聚簇索引的顺序存储，范围查询时只需顺序读取数据页。查询显示一定范围数据的时候，由于数据都是紧密相连，数据库不用从多个数据块中提取数据，所以节省了大量的io操作。</p><blockquote><p><strong>缺点</strong>：</p></blockquote><p><strong>插入速度严重依赖于插入顺序</strong>：按照主键的顺序插入是最快的方式，否则将会出现页分裂，严重影响性能。因此，对于Inn哦DB表，我们一般都会定义一个自增的ID列为主键。</p><p><strong>更新主键的代价高</strong>：因为将会导致被更新的行移动。</p><p><strong>占用较大存储空间</strong>：聚簇索引存储的是完整数据行，索引结构占用更多空间。</p><p><strong>更新和插入开销较大</strong>：由于聚簇索引将数据存储顺序固定为主键顺序，插入新数据或更新主键时可能需要重新组织数据页，影响性能。</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token comment">-- 创建一个带有主键的表</span></span>
<span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> employees <span class="token punctuation">(</span></span>
<span class="line">    employee_id <span class="token keyword">INT</span> <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span><span class="token punctuation">,</span>  <span class="token comment">-- 主键作为聚簇索引</span></span>
<span class="line">    name <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">50</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    department_id <span class="token keyword">INT</span></span>
<span class="line"><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在此表中，<code>employee_id</code> 列作为主键，因此 <code>employee_id</code> 成为聚簇索引的键，数据存储会按照该列的顺序排列。</p><h3 id="b-二级索引" tabindex="-1"><a class="header-anchor" href="#b-二级索引"><span>b.二级索引</span></a></h3><p><strong>二级索引</strong>（Secondary Index），也称为<strong>辅助索引</strong>或<strong>非聚簇索引</strong>，与聚簇索引不同，其叶子节点存储的并不是完整的数据行，而是指向聚簇索引中数据的位置。</p><p>索引中数据的位置。</p><ol><li><strong>独立于数据存储顺序</strong>：二级索引的键值与数据的物理存储顺序无关，它仅用于加速对特定列的查询操作。</li><li><strong>叶子节点存储指向聚簇索引的指针</strong>：在 InnoDB 中，二级索引的叶子节点包含聚簇索引键的值，而不是完整的数据行。因此，通过二级索引查找数据时，先通过索引定位到聚簇索引键，再访问聚簇索引以获取完整数据行。</li><li><strong>允许多个二级索引</strong>：一个表可以有多个二级索引，每个索引加速不同列上的查询。</li></ol><blockquote><p>优点</p></blockquote><ul><li><strong>查询优化</strong>：适用于加速非主键列上的查询，尤其是在 <code>WHERE</code>、<code>ORDER BY</code> 和 <code>GROUP BY</code> 操作中。</li><li><strong>灵活性高</strong>：可以根据不同查询需求添加多个二级索引。</li></ul><blockquote><p>缺点</p></blockquote><ul><li><strong>访问成本较高</strong>：查询时需要先定位聚簇索引，再通过聚簇索引找到完整数据，称为“回表”操作。</li><li><strong>维护成本</strong>：插入、更新和删除数据时，需要同时维护二级索引，影响写操作的性能。</li></ul><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token comment">-- 创建一个表，带有二级索引</span></span>
<span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> employees <span class="token punctuation">(</span></span>
<span class="line">    employee_id <span class="token keyword">INT</span> <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span><span class="token punctuation">,</span></span>
<span class="line">    name <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">50</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    department_id <span class="token keyword">INT</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token keyword">INDEX</span> idx_department <span class="token punctuation">(</span>department_id<span class="token punctuation">)</span>  <span class="token comment">-- 二级索引</span></span>
<span class="line"><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在此示例中，<code>department_id</code> 列上创建的 <code>idx_department</code> 是二级索引。通过 <code>department_id</code> 查询时，MySQL 首先通过 <code>idx_department</code> 索引找到对应的主键值，然后再查找聚簇索引以获取完整的数据行。</p><blockquote><p>回表：</p><p>在查询时使用了二级索引，如果查询的数据能在二级索引里查询的到，那么就不需要回表，这个过程就是覆盖索引。如果查询的数据不在二级索引里，就会先检索二级索引，找到对应的叶子节点，获取到主键值后，然后再检索主键索引，就能查询到数据了，这个过程就是回表。</p><p>回表是为了控制非聚簇索引的大小，如果非聚簇索引直接存储数据，所有索引都会包含相同的数据，这会导致大量的数据冗余。每个索引都需要重复存储相同的行数据，浪费大量的存储空间。</p></blockquote><h3 id="c-聚簇索引和二级索引的对比" tabindex="-1"><a class="header-anchor" href="#c-聚簇索引和二级索引的对比"><span>c.聚簇索引和二级索引的对比</span></a></h3><table><thead><tr><th>特点</th><th>聚簇索引</th><th>二级索引</th></tr></thead><tbody><tr><td>数据存储方式</td><td>索引和数据存储在一起</td><td>仅存储指向聚簇索引的指针</td></tr><tr><td>数据访问效率</td><td>查询效率高，不需要回表</td><td>需要回表操作，查询速度稍慢</td></tr><tr><td>索引数量限制</td><td>每张表只能有一个</td><td>可在表上创建多个</td></tr><tr><td>更新和插入性能</td><td>插入和更新成本高，可能需要重新排列数据页</td><td>维护多个二级索引增加写入开销</td></tr><tr><td>适用场景</td><td>主键查询和范围查询</td><td>非主键列的查询</td></tr></tbody></table><ul><li><strong>聚簇索引</strong>将数据存储与索引结合，查询主键时效率更高，适合范围查询。</li><li><strong>二级索引</strong>独立于数据的存储顺序，为非主键列加速查询，适用于多样化的查询需求，但访问数据时可能需要回表操作。</li></ul><h2 id="_2、按数据结构分类" tabindex="-1"><a class="header-anchor" href="#_2、按数据结构分类"><span>2、按数据结构分类</span></a></h2><p>从数据结构的角度来看，MySQL 常见索引有 B+Tree 索引、HASH 索引、全文索引。</p><p>B+Tree 是 MySQL 中使用最广泛的索引类型，适用于 InnoDB 和 MyISAM 等常见存储引擎。</p><table><thead><tr><th>索引类型</th><th>InnoDB</th><th>MyISAM</th></tr></thead><tbody><tr><td>B+Tree索引</td><td>是</td><td>是</td></tr><tr><td>Hash索引</td><td>否</td><td>否</td></tr><tr><td>全文索引</td><td>是</td><td>是</td></tr></tbody></table><p>在创建表时，InnoDB 存储引擎根据不同情况选择列作为聚簇索引的键（key）：</p><ul><li>如果表有主键，则默认使用主键作为聚簇索引的键；</li><li>若没有主键，则选择第一个不包含 NULL 值的唯一列作为聚簇索引的键；</li><li>如果既没有主键，也没有不含 NULL 值的唯一列，InnoDB 会自动生成一个隐式自增 id 列作为聚簇索引的键。</li></ul><p>除了聚簇索引，其他索引被称为辅助索引（二级索引或非聚簇索引）。<strong>InnoDB 中的主键索引和二级索引默认使用 B+Tree 索引</strong>。</p><ul><li><strong>B+树索引</strong>：只有叶子节点存储 value，非叶子节点只有指针和 key。</li><li><strong>哈希索引</strong>：类似键值对的形式，一次即可定位。</li><li><strong>全文索引</strong>：对文本的内容进行分词，进行搜索。目前只有 <code>CHAR</code>、<code>VARCHAR</code> ，<code>TEXT</code> 列上可以创建全文索引。一般不会使用，效率较低，通常使用搜索引擎如 ElasticSearch 代替。</li></ul><h3 id="a-hash索引" tabindex="-1"><a class="header-anchor" href="#a-hash索引"><span>a.Hash索引</span></a></h3><p>哈希表是键值对的集合，通过键即可快速取出对应的值，因此哈希表可以快速检索数据（接近 O（1））。</p><p>Hash索引基于Hash表实现，只有查询条件精确匹配Hash索引中的列时，才能够使用到hash索引。对于Hash索引中的所有列，存储引擎会为每行计算一个hashcode，Hash索引中存储的就是hashcode。</p><p>通过<strong>哈希算法</strong>（也叫散列算法），我们可以快速找到 key 对应的 index，找到了 index 也就找到了对应的 value。</p><div class="language-c++ line-numbers-mode" data-highlighter="prismjs" data-ext="c++" data-title="c++"><pre><code><span class="line">hashcode = hashfunc(key);</span>
<span class="line">index = hash % arr.size();</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>例如一个维护了卡号和姓名的表，根据卡号查询名字，其Hash索引如下：</p><p><img src="`+l+`" alt=""></p><p>但是，哈希算法存在Hash冲突问题，也就是说多个不同的 key 最后得到的 index 相同。通常情况下，我们常用的解决办法是 <strong>链地址法</strong>。链地址法就是将哈希冲突数据存放在链表中。</p><p>为了减少 Hash 冲突的发生，一个好的哈希函数应该“均匀地”将数据分布在整个可能的哈希值集合中。</p><blockquote><p>HASH索引|可以一次定位，不需要像树形索引那样逐层查找，因此具有极高的效率。但只在”=“和”in“条件下才高效，对于范围查询、排序以组合索引仍然效率不高。</p><p>由于存储的值不一定是递增的，这样在新增数据时，只需要在后面追加。因此在范围查询的时候查询速度很慢。</p><p>但由于 Hash 索引不支持顺序和范围查询。假如我们要对表中的数据进行排序或者进行范围查询，那 Hash 索引可就不行了。并且，每次 IO 只能取一个。</p></blockquote><h3 id="b-全文索引" tabindex="-1"><a class="header-anchor" href="#b-全文索引"><span>b.全文索引</span></a></h3><p>全文索引是目前==搜索引擎==使用的一种关键技术。它能够利用【分词技术】等多种算法智能分析出文本文字中关键词的频率和重要性，然后按照一定的算法规则智能地筛选出我们想要的搜索结果。全文索引非常适合大型数据集，对于小的数据集，它的用处比较小。</p><p>使用参数<code>FULLTEXT</code>可以设置索引为全文索引。在定义索引的列上支持值的全文查找，允许在这些索引引列中插入重复值和空值。全文索引只能创建在<code>CHAR</code>、<code>VARCHAR</code>或<code>TEXT</code>类型及其系列类型的字段上，查询数据量较大的字符串类型的字段时，使用全文素引可以提高查询速度。例如，表<code>student</code>的字段<code>information</code>是<code>TEXT</code>类型，该字段包含了很多文字信息。在字段<code>information</code>上建立全文索引后，可以提高查询字段<code>information</code>的速度。</p><h3 id="c-b-树索引" tabindex="-1"><a class="header-anchor" href="#c-b-树索引"><span>c.B+树索引</span></a></h3><p><strong>B+树索引</strong>是一种基于 B+树数据结构实现的索引类型，是 MySQL 默认采用的索引结构。MySQL 中的 InnoDB 存储引擎默认采用 B+树作为聚簇索引结构。</p><p>B+Tree 的主要特点及其在数据库中的重要性：</p><ol><li><strong>多路平衡树</strong>：B+Tree 是一种多路平衡树，每个节点可以包含多个元素和子节点，从而减少树的深度，加快查找速度。</li><li><strong>叶子节点链表</strong>：所有叶子节点形成一个双向链表结构，相邻的叶子节点相互连接，便于范围查询。查询数据时可以顺序遍历叶子节点，减少磁盘访问次数。</li><li><strong>路径平衡</strong>：B+Tree 的根节点到叶子节点路径长度相同，保持了树的平衡性，从而提高了查询效率。</li></ol><p>B+树全称：多路平衡搜索树，减少磁盘访问次数；用来组织磁盘数据，以页为单位，物理磁盘页一般为4K，innodb 默认页大小为 16K；对页的访问是一次磁盘io，缓存中会缓存常访问的页；</p><p>==非叶子节点只存储索引信息，叶子节点存储具体数据信息==。叶子节点之间互相连接，方便范围查询。</p><p>(图片/索引/B.png)</p><p>在 InnoDB 中，默认的 B+树节点大小为 16 KB。这意味着每个节点（无论是叶子节点还是非叶子节点）都可以存储最多 16 KB 的数据。对于 B+树中的非叶子节点，每个节点存储的是键值和指向下一个节点的指针。而在叶子节点中，存储的是数据记录。</p><p>我们假定：每个键值大小为10字节，指针大小为6字节，每一行大小为1KB，每个节点大小为16KB。</p><p>一个非叶子节点存储的内容为一组键值（key）和指向下一个节点的指针（pointer）。每个非叶子节点存储的键值和指针总共占用 10 字节（key）+ 6 字节（pointer）= 16 字节。</p><p>因此，在 16 KB 的节点大小限制下，一个非叶子节点最多可以存储：</p><div class="language-scss line-numbers-mode" data-highlighter="prismjs" data-ext="scss" data-title="scss"><pre><code><span class="line">16 KB <span class="token operator">/</span> 16 字节 = 1024 个 <span class="token punctuation">(</span>key <span class="token operator">+</span> pointer<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>每个叶子节点存储的是数据行，每一行数据的大小为 1 KB。由于每个叶子节点的大小为 16 KB，所以每个叶子节点最多可以存储：</p><div class="language-scss line-numbers-mode" data-highlighter="prismjs" data-ext="scss" data-title="scss"><pre><code><span class="line">16 KB <span class="token operator">/</span> 1 KB = 16 行数据</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>但是在实际情况中，存储的行数可以更高，因为不仅仅是存储数据行，还可能有其他元数据（如指向下一个叶子节点的指针），所以每个叶子节点能存储的行数会更多。假设每个叶子节点能够存储 1024 行数据。</p><p>根据上面的计算，我们可以通过 B+树的层数来估算最大存储能力。</p><blockquote><p><strong>2 层 B+树</strong></p></blockquote><ul><li>根节点：一个非叶子节点可以存储 1024 个键值和指针；</li><li>叶子节点：每个叶子节点可以存储 1024 行数据。</li></ul><p>因此，2 层 B+树的总记录数为：</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line">1024 * 16 = 16384 条记录</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><blockquote><p><strong>3 层 B+树</strong></p></blockquote><ul><li>根节点：可以存储 1024 个键值和指针；</li><li>第二层节点：可以存储 1024 个指向叶子节点的指针；</li><li>叶子节点：每个叶子节点可以存储 1024 行数据。</li></ul><p>因此，3 层 B+树的总记录数为：</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line">1024 * 1024 * 16 = 1677 7216 条记录</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><blockquote><p><strong>4 层 B+树</strong></p></blockquote><ul><li>根节点：可以存储 1024 个键值和指针；</li><li>第二层节点：可以存储 1024 个指向第三层节点的指针；</li><li>第三层节点：可以存储 1024 个指向叶子节点的指针；</li><li>叶子节点：每个叶子节点可以存储 1024 行数据。</li></ul><p>因此，4 层 B+树的总记录数为：</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line">1024 * 1024 * 1024 * 16 = 171 7986 9184 条记录</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>数据库的索引和数据都是存储在硬盘的，我们可以把读取一个节点当作一次磁盘 I/O 操作。那么上面的整个查询过程一共经历了 3 个节点，也就是进行了 3 次 I/O 操作。</p><p>B+Tree 存储千万级的数据只需要 3-4 层高度就可以满足，这意味着从千万级的表查询目标数据最多需要 3-4 次磁盘 I/O，所以<strong>B+Tree 相比于 B 树和二叉树来说，最大的优势在于查询效率很高，因为即使在数据量很大的情况，查询一个数据的磁盘 I/O 依然维持在 3-4次</strong>。</p><h3 id="d-小结" tabindex="-1"><a class="header-anchor" href="#d-小结"><span>d.小结</span></a></h3><p><strong>B+树与 B 树的区别</strong></p><ul><li><strong>数据存储</strong><ul><li><strong>B 树</strong>：在 B 树中，非叶子节点除了存储指针外，还存储数据。也就是说，B 树的每个节点都包含了完整的键值和对应的数据记录。</li><li><strong>B+树</strong>：与 B 树不同，B+树的非叶子节点仅存储键值和指向子节点的指针，而数据只存储在叶子节点。由于叶子节点只存储数据，这样可以使得非叶子节点的存储容量更大，从而减少树的高度，提高查询效率。</li></ul></li><li><strong>单节点的数据量</strong><ul><li><strong>B+树</strong>：由于非叶子节点不存储数据，单个节点的存储容量更大。因此，在相同的磁盘 I/O 次数下，B+树能够查询更多的节点。</li><li><strong>B 树</strong>：每个非叶子节点都需要存储数据，因此每个节点的存储量较小，导致树的高度可能更高，查询效率较低。</li></ul></li></ul><p><strong>B+树的叶子节点双向链表</strong></p><p>B+树的叶子节点之间通过双向链表连接，这使得 B+树非常适合范围查询。例如，使用 <code>BETWEEN</code>、<code>&gt;</code>、<code>&lt;</code> 等范围查询时，查询开始后可以通过叶子节点的链表顺序访问相邻的叶子节点，极大提高了范围查询的效率。</p><p><strong>B 树</strong>没有类似的结构，其叶子节点没有双向链表，因此无法像 B+树那样高效地进行顺序查找。</p><p><strong>B+树与二叉树的对比</strong></p><ul><li><strong>二叉树</strong>的每个节点最多有两个子节点，因此其搜索的复杂度为 <code>O(logN)</code>，在处理大量数据时，搜索过程中的磁盘 I/O 次数更多，查询效率较低。</li><li><strong>B+树</strong>的每个节点有更多的子节点，因此它的搜索复杂度较低，树的高度较小，查询过程中磁盘 I/O 次数较少，性能更高。</li></ul><p><strong>B+树与哈希表的对比</strong></p><ul><li><strong>哈希表</strong>：哈希表在进行等值查询时的效率非常高，搜索复杂度为 <code>O(1)</code>。哈希表适用于快速的等值查找，但对于范围查询则无能为力，因为哈希表的存储方式不保持元素的顺序。</li><li><strong>B+树</strong>：与哈希表不同，B+树不仅支持等值查询（如 <code>=</code> 和 <code>IN</code>），还支持范围查询（如 <code>BETWEEN</code>、<code>&gt;</code>、<code>&lt;</code>）。因此，B+树索引适用于更加广泛的场景，尤其是在需要处理范围查询和排序操作时，B+树的性能远优于哈希表。</li></ul><p><strong>B+树</strong>：适用于需要高效的范围查询、排序以及频繁的插入/删除操作的场景。例如，在数据库中，B+树通常用作索引结构来处理这些操作。</p><p><strong>哈希表</strong>：适用于快速的等值查询（如精确匹配）场景，但不适合范围查询或排序。</p><p>B+树的结构非常适合用作数据库索引，特别是在处理大规模数据时，能够提供较低的树高，减少磁盘 I/O 次数，从而提高查询效率。</p><p>哈希表适合进行快速的等值查询，但不支持范围查询，因此它在数据库索引中应用有限。</p><p>B+树的双向链表结构和较低的树高，使得它在需要高效查询、排序和范围查询的场景中比 B 树、二叉树和哈希表更具优势。</p><p>通过这些特性，B+树在 MySQL 等数据库系统中成为了默认的索引结构，帮助数据库实现高效的数据存储和检索。</p><h2 id="_3、按功能逻辑分类" tabindex="-1"><a class="header-anchor" href="#_3、按功能逻辑分类"><span>3、按功能逻辑分类</span></a></h2><p>从功能逻辑的角度来看，索引分为普通索引、唯一索引、主键索引、前缀索引。</p><h3 id="a-普通索引" tabindex="-1"><a class="header-anchor" href="#a-普通索引"><span>a.普通索引</span></a></h3><p>在创建普通索引时，不附加任何限制条件，只是用于提高查询效率。这类索引可以创建在任何数据类型中，其值是否唯一和非空，要由字段本身的完整性约束条件决定。建立索引以后，可以通过索引进行查询。例如，在表<code>student</code>的字段<code>name</code>上建立一个普通索引，查询记录时就可以根据该索引进行查询。</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token comment">-- 在创建表时添加普通索引</span></span>
<span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> employees <span class="token punctuation">(</span></span>
<span class="line">    id <span class="token keyword">INT</span><span class="token punctuation">,</span></span>
<span class="line">    name <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">50</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    department_id <span class="token keyword">INT</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token keyword">INDEX</span> idx_department_id <span class="token punctuation">(</span>department_id<span class="token punctuation">)</span>  <span class="token comment">-- 普通索引</span></span>
<span class="line"><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">-- 在已存在的表上添加普通索引</span></span>
<span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">INDEX</span> idx_name <span class="token keyword">ON</span> employees<span class="token punctuation">(</span>name<span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>创建普通索引后，MySQL 会在执行涉及索引列的查询时自动利用索引，显著提升数据检索的效率。</p><p>普通索引通常用于需要快速查找或频繁查询的数据列。例如，频繁用于 <code>WHERE</code> 子句筛选的列或排序操作的列，通常适合添加普通索引。</p><h3 id="b-唯一索引" tabindex="-1"><a class="header-anchor" href="#b-唯一索引"><span>b.唯一索引</span></a></h3><p>使用<code>UNIQUE</code>参数可以设置索引为唯一性索引，在创建唯一性索引时，限制该索引的值必须是唯一的，但允许有空值。在一张数据表里可以有多个唯一索引。</p><blockquote><p>凡是声明了唯一性约束的字段，自动添加了唯一索引。</p></blockquote><p>例如，在表<code>student</code>的字段<code>email</code>中创建唯一性索引，那么字段<code>email</code>的值就必须是唯一的。通过唯一性索引，可以更快速地确定某条记录。</p><h3 id="c-主键索引" tabindex="-1"><a class="header-anchor" href="#c-主键索引"><span>c.主键索引</span></a></h3><p>主键索引I就是一种特殊的唯一性索引，在唯一索引的基础上增加了不为空的约束，也就是<code>NOT NULL</code>+<code> UNIQUE</code>，一张表里最多只有一个主键索引。这是由主键索引的物理实现方式决定的，因为数据存储在文件中只能按照一种顺序进行存储。</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token comment"># 创建</span></span>
<span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> student <span class="token punctuation">(</span></span>
<span class="line">    id <span class="token keyword">INT</span><span class="token punctuation">(</span><span class="token number">10</span><span class="token punctuation">)</span> <span class="token keyword">UNSIGNED</span>  <span class="token keyword">AUTO_INCREMENT</span> <span class="token punctuation">,</span></span>
<span class="line">    student_no <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">200</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    student_name <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">200</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span> </span>
<span class="line"><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token comment"># 删除</span></span>
<span class="line"><span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> student <span class="token keyword">drop</span> <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">;</span>  </span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>修改主键索引时，必须先删除掉原索引，再新建索引。</p><h3 id="d-前缀索引" tabindex="-1"><a class="header-anchor" href="#d-前缀索引"><span>d.前缀索引</span></a></h3><p><strong>前缀索引</strong>是一种节省空间的索引方式，主要用于长文本或字符串列。创建索引时，前缀索引只索引字段值的前部分字符，而不是整个字段值。这种方式可以显著减少索引的存储空间，并在查询大量数据时提升性能，适用于文本类型字段（例如 <code>CHAR</code>、<code>VARCHAR</code> 和 <code>TEXT</code>）。</p><p>假设我们有一张用户信息表 <code>users</code>，其中包含一个 <code>email</code> 字段。由于 <code>email</code> 字段可能较长，因此可以对其创建前缀索引来加速查询。</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> users <span class="token punctuation">(</span></span>
<span class="line">    id <span class="token keyword">INT</span> <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span><span class="token punctuation">,</span></span>
<span class="line">    username <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">50</span><span class="token punctuation">)</span><span class="token punctuation">,</span></span>
<span class="line">    email <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment">-- 创建一个长度为 10 的前缀索引</span></span>
<span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">INDEX</span> idx_email <span class="token keyword">ON</span> users<span class="token punctuation">(</span>email<span class="token punctuation">(</span><span class="token number">10</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这个示例中，我们只对 <code>email</code> 字段的前 10 个字符建立索引。这样在查询类似 <code>email LIKE &#39;user@example.com%&#39;</code> 的数据时，可以使用该索引加快查询速度。</p><h2 id="_4、按字段个数逻辑分类" tabindex="-1"><a class="header-anchor" href="#_4、按字段个数逻辑分类"><span>4、按字段个数逻辑分类</span></a></h2><h3 id="a-单列索引" tabindex="-1"><a class="header-anchor" href="#a-单列索引"><span>a.单列索引</span></a></h3><p>在表中的单个字段上创建索引。单列索引只根据该字段进行索引。单列索引可以是普通索引，也可以是唯一性索引，还可以是全文索引。只要保证该索引只对应一个字段即可。一个表可以有多个单列索引。</p><h3 id="b-联合索引" tabindex="-1"><a class="header-anchor" href="#b-联合索引"><span>b.联合索引</span></a></h3><p>联合索引是在表的多个字段组合上创建一个索引。该索引指向创建时对应的多个字段，可以通过这几个字段进行查询，但是只有查询条件中使用了这些字段中的第一个字段时才会被使用。例如，在表中的字段<code>id</code>、<code>name</code>和<code>gender</code>上建立一个多列索引<code>idx_id_name_gender</code>，只有在查询条件中使用了字段<code>id</code>时该索引才会被使用。使用组合索引时遵循<strong>最左前缀集合</strong>。</p><hr><h1 id="三、索引常见面试题" tabindex="-1"><a class="header-anchor" href="#三、索引常见面试题"><span>三、索引常见面试题</span></a></h1><blockquote><p><strong>InnoDB与MyISAM索引实现的区别</strong></p></blockquote><ol><li><strong>MyISAM的索引方式都是非聚簇索引，而InnoDB包含一个聚簇索引。</strong></li></ol><p>也就是说，InnoDB中我们只需要根据主键值对聚簇索引进行一次查找就能找到记录，而MyISAM却需要进行一次回表操作。</p><ol start="2"><li><strong>InnoDB的数据文件本身就是索引文件，而MyISAM索引文件和数据文件是分离的，索引文件仅保存数据记录的地址。</strong></li></ol><p>MyISAM的表在磁盘上存储在以下文件中：<code>*.sdi</code>（描述表结构）、<code>*.MYD</code>（数据），<code>.MYI</code>（索引）。</p><p>InnoDB的表在磁盘上存储在以下文件中：<code>.ibd</code>（表结构+索引+数据都存储在一起）。数据和索引存储在同一个结构中，所有的数据存储都按照主键顺序进行。</p><ol start="3"><li><strong>InnoDB的非聚簇索引Idata域存储相应记录主键的值，而MyISAM索引记录的是地址。</strong></li></ol><p>MyISAM的叶子节点中存储的键值为索引列的值，数据为索引所在行的磁盘地址。数据存储在单独的文件中，查询数据时通过索引找到数据文件的物理位置。</p><p>InnoDB的聚簇索引的叶子节点存储的是数据行本身。非聚簇索引的叶子节点存储的是主键值，而不是数据行。当通过二级索引查找数据时，必须通过主键再去访问数据。</p><p>因此MyISAM的回表速度是很快的，直接拿着地址偏移量到文件中取数据。而Innodb是通过获取主键之后再去聚簇索引中查找。</p><blockquote><p><strong>面试题：为什么 InnoDB 要使用 B+ 树作为索引？</strong></p></blockquote><ul><li><p>更高效的磁盘 IO，因为它减少了磁盘寻道时间和页的加载次数</p></li><li><p><strong>支持范围查询</strong>，与 B 树相比，B+ 树的叶子结点通过指针连接成一个链表，这使得查询范围变得非常高效。在 B+ 树上执行范围查询可以简单地从范围的起始点开始，然后沿着链表向后遍历，直到结束点。</p></li><li><p><strong>查询性能稳定</strong>，B+ 树的所有查找操作都要查到叶子节点，这使得所有的查询操作都有着相同的访问深度，因此查询性能非常稳定。不像某些其他数据结构，如 B 树，其查询性能因为数据存在所有的节点上导致深度不一致，性能不稳定。</p></li></ul><blockquote><p><strong>面试题：索引是不是建的越多越好？</strong></p></blockquote><p>不是。</p><ul><li>索引会占据磁盘空间</li><li>每当在表中插入、删除或更新数据时，数据库系统都需要更新所有相关的索引。</li></ul><blockquote><p><strong>面试题：索引不适合哪些场景呢？</strong></p></blockquote><ul><li>数据表很小：如果数据表的记录数量非常少，数据库进行全表扫描的成本可能比维护和查询索引的成本要低。在这种情况下，索引可能不会带来性能提升。</li><li>频繁更新的索引列：如果一个列的值经常更改，每次更改都可能导致索引的重建或重排，这可以增加大量的性能负担。</li></ul><blockquote><p><strong>面试题：索引在哪些情况下会失效？</strong></p></blockquote><ul><li><strong>非最左前缀使用</strong>：大多数数据库的索引（特别是符合索引）是基于最最左前缀原则构建的。如果查询不从索引的最左列开始，索引可能不会被使用。例如，索引是在 <code>(lastname, firstname)</code> 上，但查询只有关于 <code>firstname</code> 的条件</li><li><strong>使用 <code>LIKE</code> 运算符时的前导通配符</strong>：如果查询使用 <code>LIKE</code> 且模式以通配符开始（如 <code>LIKE &#39;%Smith&#39;</code>），则索引可能不能使用，因为数据库无法利用索引有效地查找这种模式。</li><li><strong>数据类型不匹配</strong>：如果查询条件中的数据类型与索引列的数据类型不匹配，数据库可能需要进行隐式类型转换，这可能导致索引失效。例如，如果索引列是整数类型，但查询条件用字符串来比较，如<code>WHERE column = &#39;123&#39;</code>。</li><li><strong><code>NULL</code> 值</strong>：某些数据库处理包含 NULL 值的列的索引时可能会有问题。如果索引列有很多 NULL 值，或者查询条件包含对 NULL 值的比较，索引的效果可能会降低。</li><li><strong><code>OR</code> 条件</strong>：使用<code>OR</code>连接的查询条件如果不是每个条件都在同一个索引中，可能导致索引失效。例如，如果有两个条件分别使用不同的索引，数据库可能决定放弃使用索引。</li><li><strong>使用不等式运算符</strong>：虽然索引可以用于不等式查询（如<code>&gt;</code>、<code>&lt;</code>等），但如果这样的查询返回表中大部分数据，数据库优化器可能决定全表扫描更有效，因此不使用索引。</li><li><strong>排序和聚合操作</strong>：如果查询包括排序（<code>ORDER BY</code>）或聚合（如<code>GROUP BY</code>、<code>MAX()</code>、<code>COUNT()</code>等）操作，且这些操作所涉及的列没有相应的索引支持，那么执行这些操作时索引可能不被使用。</li></ul><blockquote><p><strong>面试题：什么是索引下推（ICP）</strong></p></blockquote><p>Index Condition Pushdown(ICP)是MySQL5.6中新特性，是一种在存储引擎层使用索引过滤数据的优化方式。</p><p>在传统的查询执行中，MySQL会先通过索引扫描定位到符合条件的行，然后再将剩余的条件应用到结果集中过滤符合要求的数据。索引下推技术的核心思想是，将某些查询条件直接下推到索引扫描的过程中，而不是等到全表扫描或回表时再进行过滤。</p><p>具体来说，索引下推会将能够使用索引的条件直接下推到存储引擎层面，减少回表和额外的过滤工作。例如，索引条件下推可以将<code>WHERE</code>子句中的某些条件通过索引直接进行筛选，而不是在查询结果返回后再做筛选。</p><p>它的作用是将查询中的一部分条件从查询的外部传递到索引扫描阶段进行处理，以减少不必要的数据读取，从而提高查询性能。</p><ul><li>如果没有ICP，存储引擎会遍历索引以定位基表中的行，并将它们返回给MySQL服务器，由MySQL服务器评估<code>WHERE</code>后面的条件是否保留行。</li><li>启用ICP后，如果部分<code>WHERE</code>条件可以仅使用索引I中的列进行筛选，则MySQL服务器会把这部分<code>WHERE</code>条件放到存储引擎筛选。然后，存储引擎通过使用索引条目来筛选数据，并且只有在满足这一条件时才从表中读取行。 <ul><li>好处：ICP可以减少存储引擎必须访问基表的次数和MySQL服务器必须访问存储引擎的次数。</li><li>但是，ICP的加速效果取决于在存储引擎内通过ICP筛选掉的数据的比例。</li></ul></li></ul><p>更多的是针对联合索引。</p><blockquote><p>适用场景</p></blockquote><ol><li><strong>范围条件（Range Conditions）</strong>：例如<code>WHERE age &gt; 30 AND age &lt; 40</code>，索引可以帮助快速定位到符合年龄范围的记录。</li><li><strong>多列组合条件</strong>：当查询涉及多个列时，索引下推可以让MySQL在扫描索引时就判断多个条件，而不是全表扫描后再过滤。</li><li><strong>复杂查询条件</strong>：对于复杂的<code>WHERE</code>子句，索引下推有助于减少MySQL的计算量，避免扫描大量不符合条件的行。</li></ol><p>假设有一个表<code>users</code>，包含<code>id</code>、<code>age</code>、<code>gender</code>等字段，并在<code>age</code>列上创建了索引。以下查询：</p><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> users <span class="token keyword">WHERE</span> age <span class="token operator">&gt;</span> <span class="token number">25</span> <span class="token operator">AND</span> gender <span class="token operator">=</span> <span class="token string">&#39;M&#39;</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>在没有索引下推的情况下，MySQL可能会首先扫描索引查找<code>age &gt; 25</code>的记录，然后再对结果集进行<code>gender = &#39;M&#39;</code>的过滤。启用索引下推后，MySQL会将<code>gender = &#39;M&#39;</code>的条件下推到索引扫描阶段，这样只会扫描符合<code>age &gt; 25</code>和<code>gender = &#39;M&#39;</code>两个条件的记录，从而减少了不必要的过滤。</p><ol><li><strong>减少回表操作</strong>：通过索引下推，能够直接通过索引完成条件的判断，减少回表查询的次数。</li><li><strong>减少IO操作</strong>：减少不符合条件的记录被读取，提高了查询效率。</li><li><strong>提高查询性能</strong>：特别是在多条件查询或复杂查询中，索引下推能显著提高查询效率。</li></ol><blockquote><p><strong>面试题：<code>COUNT(*)</code>和 <code>COUNT(具体字段)</code>效率？</strong></p></blockquote><p>如果你要统计的是某个字段的非空数据行数，则另当别论。毕竟比较执行效率的前提是结果一样才可以。</p><p><strong><code>COUNT(*)</code> 和 <code>COUNT(1)</code> 的效率</strong></p><ul><li><strong>本质区别</strong>：<code>COUNT(*)</code> 和 <code>COUNT(1)</code> 在功能上几乎是等价的，二者都用于统计表中的行数。<code>COUNT(*)</code>会统计所有行，包括字段值为<code>NULL</code>的行，而<code>COUNT(1)</code>则是统计所有行（无论字段值如何）。从查询执行的角度看，二者没有实质性的区别，因为<code>COUNT(1)</code>只是对每一行执行一个常量计数，实际上会被优化成与<code>COUNT(*)</code>相同的执行计划。</li><li><strong>查询优化</strong>：在执行过程中，MySQL的查询优化器会将<code>COUNT(*)</code>和<code>COUNT(1)</code>转换为相同的查询计划，通常两者的性能是相同的。因此，除非特殊情况下，二者的查询效率可以视为相同。</li></ul><p><strong><code>COUNT(具体字段)</code> 的效率</strong></p><ul><li><strong>统计非<code>NULL</code>值的行</strong>：<code>COUNT(字段)</code>只统计该字段值非<code>NULL</code>的行数。这意味着，<code>COUNT(字段)</code>会忽略<code>NULL</code>值，并且根据字段的实际内容来决定哪些行会被计数。</li><li><strong>影响性能的因素</strong>：在InnoDB存储引擎中，如果该字段有索引（尤其是二级索引），MySQL会尽量使用索引来进行统计。这会比扫描整个表要高效得多。特别是在没有<code>NULL</code>值的情况下，使用二级索引进行统计可以避免全表扫描。 <ul><li><strong>使用二级索引</strong>：如果字段有二级索引，MySQL会优先使用该索引来统计行数，因为二级索引的大小通常小于主键索引（聚簇索引），因此扫描速度更快。</li><li><strong>没有二级索引</strong>：如果没有二级索引，MySQL将使用聚簇索引进行扫描，聚簇索引会读取更多的数据，因为它包括了完整的行数据。这样就可能导致效率降低，尤其是在数据量很大的表中。</li></ul></li></ul><p><strong>存储引擎的差异：MyISAM vs InnoDB</strong></p><ul><li><strong>MyISAM存储引擎</strong>：MyISAM存储引擎在表的<code>meta</code>数据中存储了行数信息。查询表的行数时，MyISAM不需要扫描整个表，而是直接通过元数据获取行数，这使得查询的复杂度为<code>O(1)</code>，即常数时间复杂度。因此，在MyISAM引擎下，查询行数非常高效。</li><li><strong>InnoDB存储引擎</strong>：InnoDB由于支持事务和行级锁，不能像MyISAM那样维护行数的统计信息。因此，在InnoDB引擎下，统计表的行数时需要扫描整个表，复杂度为<code>O(n)</code>，其中<code>n</code>是表中的行数。因此，InnoDB统计行数的效率要低于MyISAM。</li></ul><p><strong><code>COUNT(*)</code> 和 <code>COUNT(1)</code></strong>：这两种方式基本等价，执行效率几乎相同。它们不依赖字段内容，仅仅对所有行进行计数，因此执行效率相当。使用哪个取决于个人偏好或规范。而**<code>COUNT(具体字段)</code>**：该方式统计字段非<code>NULL</code>值的行数，效率较低，因为它需要考虑字段的内容和索引的使用情况。如果字段有索引，尤其是二级索引，查询效率较高；如果没有索引，则可能需要扫描整个表，导致效率低下。</p><p>如果统计的是某个字段的非<code>NULL</code>值的行数，且该字段有索引（尤其是二级索引），则使用<code>COUNT(字段)</code>会更高效。如果没有索引，<code>COUNT(*)</code>通常会更快，因为它避免了字段内容的检查。</p><p>在InnoDB存储引擎中，考虑尽量使用二级索引来提高<code>COUNT(字段)</code>的查询效率，尤其是在字段值分布不均时。</p><blockquote><p><strong>面试题：关于<code>select(*)</code></strong></p></blockquote><p>在表查询中，建议明确字段，不要使用<code>*</code>作为查询的字段列表，推荐使用SELECT&lt;字段列表&gt;查询。原因</p><ul><li>MySQL在解析的过程中，会通过查询数据字典将<code>*</code>按序转换成所有列名，这会大大的耗费资源和时间。</li><li>无法使用覆盖索引。</li></ul><blockquote><p><strong>面试题：Limit 1 对优化的影响</strong></p></blockquote><p>针对的是会扫描全表的SQL语句，如果可以确定结果集只有一条，那么加上<code>LIMIT 1</code>的时候，当找到一条结果的时候就不会继续扫描了，这样会加快查询速度。</p><p>如果数据表已经对字段建立了唯一索引引，那么可以通过索引引进行查询，不会全表扫描的话，就不需要加上<code>LIMIT 1</code>了。</p>`,194)]))}const r=s(p,[["render",c],["__file","suoyin.html.vue"]]),u=JSON.parse('{"path":"/blogs/category2/2016/suoyin.html","title":"索引","lang":"en-US","frontmatter":{"title":"索引","date":"2025/1/15","tags":["索引"],"categories":["数据库"]},"headers":[{"level":2,"title":"1、按物理存储分类","slug":"_1、按物理存储分类","link":"#_1、按物理存储分类","children":[{"level":3,"title":"a.聚簇索引","slug":"a-聚簇索引","link":"#a-聚簇索引","children":[]},{"level":3,"title":"b.二级索引","slug":"b-二级索引","link":"#b-二级索引","children":[]},{"level":3,"title":"c.聚簇索引和二级索引的对比","slug":"c-聚簇索引和二级索引的对比","link":"#c-聚簇索引和二级索引的对比","children":[]}]},{"level":2,"title":"2、按数据结构分类","slug":"_2、按数据结构分类","link":"#_2、按数据结构分类","children":[{"level":3,"title":"a.Hash索引","slug":"a-hash索引","link":"#a-hash索引","children":[]},{"level":3,"title":"b.全文索引","slug":"b-全文索引","link":"#b-全文索引","children":[]},{"level":3,"title":"c.B+树索引","slug":"c-b-树索引","link":"#c-b-树索引","children":[]},{"level":3,"title":"d.小结","slug":"d-小结","link":"#d-小结","children":[]}]},{"level":2,"title":"3、按功能逻辑分类","slug":"_3、按功能逻辑分类","link":"#_3、按功能逻辑分类","children":[{"level":3,"title":"a.普通索引","slug":"a-普通索引","link":"#a-普通索引","children":[]},{"level":3,"title":"b.唯一索引","slug":"b-唯一索引","link":"#b-唯一索引","children":[]},{"level":3,"title":"c.主键索引","slug":"c-主键索引","link":"#c-主键索引","children":[]},{"level":3,"title":"d.前缀索引","slug":"d-前缀索引","link":"#d-前缀索引","children":[]}]},{"level":2,"title":"4、按字段个数逻辑分类","slug":"_4、按字段个数逻辑分类","link":"#_4、按字段个数逻辑分类","children":[{"level":3,"title":"a.单列索引","slug":"a-单列索引","link":"#a-单列索引","children":[]},{"level":3,"title":"b.联合索引","slug":"b-联合索引","link":"#b-联合索引","children":[]}]}],"git":{"createdTime":1737739746000,"updatedTime":1737740786000,"contributors":[{"name":"zyb","email":"384201115@qq.com","commits":4}]},"filePathRelative":"blogs/category2/2016/索引.md"}');export{r as comp,u as data};
